const authState = {

};

type AuthStateType = typeof authState;

export const authReducer = (state = authState, action: any): AuthStateType => {
    switch (action.type) {
        default:
            return state
    }
};